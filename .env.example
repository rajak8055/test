# Database Configuration
# Use either DATABASE_URL (full connection string) or individual parameters

# Full PostgreSQL connection string (recommended)
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Or individual parameters
PGHOST=localhost
PGPORT=5432
PGDATABASE=your_database_name
PGUSER=your_username
PGPASSWORD=your_password

# Groq LLM API Configuration
GROQ_API_KEY=your_groq_api_key_here

# Optional: Application Configuration
LOG_LEVEL=INFO

# Common local PostgreSQL examples:
# DATABASE_URL=postgresql://postgres:password@localhost:5432/postgres
# DATABASE_URL=postgresql://postgres:admin@localhost:5432/nlp_sql_db

# For testing with default PostgreSQL setup:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=postgres
# PGUSER=postgres
# PGPASSWORD=your_postgres_password
